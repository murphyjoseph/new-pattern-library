import { StyleSheet } from 'aphrodite/no-important';

export const cssTextAlignment = StyleSheet.create({
  center: {
    textAlign: "center"
  },
  right: {
    textAlign: "right"
  },
  left: {
    textAlign: "left"
  }
})