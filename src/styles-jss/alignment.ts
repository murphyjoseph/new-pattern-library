import { createUseStyles } from 'react-jss'

export const cssTextAlignment: any = createUseStyles({
  center: {
    textAlign: "center"
  },
  right: {
    textAlign: "right"
  },
  left: {
    textAlign: "left"
  }
})