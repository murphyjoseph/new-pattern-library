import { createUseStyles } from 'react-jss'

export const cssDisplay = createUseStyles({
  block: {
    display: "block"
  },
  inlineBlock: {
    display: "inline-block"
  },
  inline: {
    display: "inline"
  }
})